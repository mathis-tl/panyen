SPEC ACTIVE — 1.3b : CORRIGER LE PÉRIMÈTRE GÉOGRAPHIQUE
DESTINATAIRE — Cursor Agent

OBJECTIF UTILISATEUR
Aligner la série mensuelle de référence sur l'ECSP 2022 : Martinique contre France
métropolitaine. Sans cette correction, la future estimation combinerait un écart
ECSP Martinique–métropole avec une évolution IPC Martinique–France entière.

ÉTAT ET DÉCISION VALIDÉE
- La spec 1.3 est terminée et le commit de référence est `e1d96ad`.
- L'ECSP alimentaire (+40 %, Fisher) compare la Martinique à la France
  métropolitaine : https://www.insee.fr/fr/statistiques/7649202
- Idbanks vérifiés, base 2025, ensemble des ménages :
  `011813726` = Martinique (`D972`), `011813717` = France entière (`FE`, historique),
  `011813720` = France métropolitaine (`FM`, nouvelle référence active).
- La collecte active devient exactement `{011813726, 011813720}`.
- Le XML déjà versionné reste immuable à son emplacement. Son ancienne paire
  `{011813726, 011813717}` est conservée et explicitement étiquetée comme historique.

AVANT DE MODIFIER
1. Lire `CLAUDE.md`, `ROADMAP.md`, `SPEC.txt`, `docs/SOURCES.md`, le collecteur,
   le parseur SDMX, `stg_ipc`, ses tests pytest/dbt et le XML uniquement via ses
   métadonnées/structure utiles. Ne pas utiliser Graphify.
2. Lire et appliquer `~/.claude/skills/tdd-adaptive/SKILL.md`.
3. Reformuler en cinq lignes maximum : erreur corrigée, paire active, traitement du
   lot historique, non-objectifs et critère de réussite.
4. L'architecture est décidée. Ne proposer une alternative que si un obstacle
   prouvé empêche de préserver et distinguer les deux types de lots.

TRAVAIL AUTORISÉ
1. Mettre à jour le collecteur pour demander, dans un seul appel, uniquement
   `011813726+011813720` depuis `2022-04`. Adapter ses tests et libellés.
2. Faire accepter au parseur exactement deux paires par fichier :
   - `{011813726, 011813717}` → `france_entiere_historique` ;
   - `{011813726, 011813720}` → `france_metropolitaine`.
   Rejeter toute paire incomplète, tout troisième idbank et toute autre combinaison.
3. Vérifier la correspondance exacte idbank/`REF_AREA` : `726/D972`, `717/FE`,
   `720/FM`. Un désaccord doit échouer bruyamment.
4. Ajouter à chaque observation `perimetre_reference`, dérivé de la paire du fichier,
   puis propager et documenter cette colonne dans `stg_ipc`.
5. Adapter les tests dbt : trois idbanks historiques autorisés au total, mais une
   seule des deux paires exactes par fichier ; cohérence du périmètre et du code
   territoire ; conserver tous les garde-fous de 1.3.
6. Corriger les faits et formulations concernés dans `CLAUDE.md`, `docs/SOURCES.md`
   et le README. France métropolitaine est la référence active ; France entière
   n'est mentionnée que comme lot historique.
7. Après les tests locaux, effectuer une seule collecte normale afin de créer le
   premier XML brut de la paire active.

TDD ET CAS OBLIGATOIRES
- Commencer par un test rouge prouvant que le collecteur utilise `011813720` au lieu
  de `011813717`, puis implémenter le changement.
- Couvrir le parsing d'un lot historique et d'un lot métropolitain, leur étiquette,
  les trois correspondances idbank/territoire, une paire incomplète, une paire
  mélangée et un troisième idbank.
- Les tests utilisent des fixtures locales et `tmp_path` : aucun réseau, aucune
  écriture dans `data/raw/`.
- Mettre à jour le test SQL « deux séries par fichier » pour contrôler les paires
  exactes, pas seulement `count(distinct idbank) = 2`.

INVARIANTS
- Ne jamais modifier, renommer ou supprimer
  `data/raw/insee/ipc_alimentation_2026-09-02T163321Z.xml` ; son SHA-256 attendu reste
  `96e9b84a449d1ff3a94729344758e4786bc0752dccb9c7c533532cd25d2d8606`.
- Le brut reste un journal : pas de parsing, correction ou filtrage dans `ingest/`.
- Grain de `stg_ipc` inchangé : `(fichier_source, idbank, periode)`.
- Ne comparer ni afficher aucun niveau d'indice entre territoires.
- Le bord de publication peut différer ; ne pas imposer le même dernier mois.

NON-OBJECTIFS
- Ne pas sélectionner le dernier snapshot actif, apparier les mois, rebaser,
  calculer une évolution, un différentiel ou extrapoler l'écart ECSP : spec 1.4.
- Ne créer aucun modèle `int_`/`fct_`, seed ECSP, Parquet, UI ou infrastructure.
- Ne pas ajouter de dépendance, plugin, skill, hook, MCP, commit, remote ou PR.

VÉRIFICATION
1. Exécuter les tests ciblés pendant le TDD, puis lire et appliquer
   `~/.claude/skills/verification-loop/SKILL.md` sans utiliser `/verify`.
2. Vérifier avant et après la collecte le SHA-256 du XML historique.
3. Lancer une seule collecte réelle : `uv run python ingest/insee_ipc.py`.
4. Exécuter `make verify`, entièrement hors réseau après cette collecte.
5. Interroger uniquement la structure et la provenance : par fichier, afficher
   `perimetre_reference`, idbanks, codes territoire, volumes et périodes min/max.
   Ne jamais afficher les valeurs d'indice.
6. Inspecter `git status --short`, le diff final et confirmer qu'il n'existe aucun
   fichier brut réécrit ou supprimé.

FINI QUAND
- Le collecteur actif produit un nouveau lot `726+720` depuis avril 2022.
- Le staging reconstruit ensemble l'ancien lot historique et le nouveau lot actif en les
  distinguant sans ambiguïté, et rejette toute combinaison territoriale invalide.
- Le XML historique est inchangé et `make verify` est vert hors réseau.

RAPPORT FINAL
- Résultat, fichiers touchés et raison ; preuve RED puis tests exacts.
- Noms et SHA-256 des deux XML, sans recopier leur contenu.
- Provenance/volumétrie/périodes par fichier et idbank, sans niveaux d'indice.
- Chemins des deux skills lus, écarts et risques.
- Puis STOP : ne pas commencer la spec 1.4 et ne pas committer.
